export interface Button {
  x: number; // Will be calculated dynamically
  y: number; // Will be calculated dynamically
  width: number;
  height: number;
  text: string;
  borderColor: string;
  hoverColor: string;
  isHovered: boolean;
}
